services:

  web:
    build: .
    container_name: sip-web-2
    ports:
      - "8080:80"           # Interfaz web por HTTP (solo localhost)
      - "8443:443"          # Interfaz web por HTTPS (necesaria desde otra red/PC, requerida por getUserMedia)
    restart: unless-stopped
